@{
    Layout = "_AdminLayout";
}
<div class="container">
    <div class="row">
        <div class="col-md-8">
            <table class="table table-striped border border-1" id="addDepartmentTable">
                <thead>
                    <tr>
                        <th>Department</th>
                        <th>Action</th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <td>
                            <form>
                                <input type="text" class="form-control" placeholder="Department Name"/>
                            </form>
                        </td>
                        <td> 
                            <button class="btn btn-primary" onclick="addTableRow();" id="submitForm">
                                    +
                            </button>
                        </td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>
</div>

@section Scripts{
    <script>
        const submitBtn = document.querySelector('#submitForm')
        submitBtn.addEventListener('click', function (e) {
            e.preventDefault()
            
        })

    </script>
}